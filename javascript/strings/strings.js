/* ==========================================================================
strings
========================================================================== */

var myString = "my sample sting";
var length = 5;
var trimmedString = myString.substring(0, length);